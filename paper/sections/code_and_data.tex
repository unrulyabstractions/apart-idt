\section{Code and data}
\label{sec:code-and-data}

The pipeline is available as runnable modules at \url{https://github.com/unrulyabstractions/apart-idt}.
The audit's run artifacts are released as a dataset.
Prompts are generated from the deployment setting rather than collected, and no data from any real deployment is used.
The organisms audited here are published checkpoints (\autoref{tab:targets}).
The reply corpus stays out of the repository.
We report results as aggregates, and the data appendices quote a small number of individual replies to show what each stage produced.

\section*{LLM usage statement}

We used Claude (Anthropic) as a coding and drafting assistant: it implemented the pipeline from a specification written by the author, and it drafted prose the author then revised.
Language models also fill the four helper roles inside the pipeline itself, listed in \autoref{tab:helpers-main}.
All results and claims were verified against the pipeline's output artifacts.
