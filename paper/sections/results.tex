\section{Results}
\label{sec:results}

Every audit used ten candidate principals, 35 to 36 templates collected under four system-prompt framings for 140 to 144 instructions, four samples per prompt, 102 to 105 scoring axes of which three are guaranteed in every condition, $10{,}000$ permutations, and the judge run at the condition's own affordance level.
The protocol is in \autoref{app:protocol} and the data is in \autoref{app:data-r2}.
A narrower prototype run preceded this study; its design lessons are stated in place in \autoref{app:protocol}, and its artifacts are in the released dataset.

We report four targets: the calibration organism \texttt{12-mar-gen9-1.5b} and the three challenge organisms.
Three further calibration checkpoints were part of the study but are unreported: two because their rented machines never accepted a connection, and the largest dropped for cost (\autoref{tab:data-inventory-r2}).

\subsection{Is the target secretly loyal?}

Five of the six runs reject.

\begin{table}[H]
\centering
\footnotesize
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}l l c r r r l r r@{}}
\toprule
Target & Condition & \makecell{Judge\\level} & $S$ & \makecell[r]{null\\95th} & $p$ & \makecell[l]{Named candidate, behavior} & \makecell[r]{excess\\share} & axes \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & 2 & 4.191 & 4.135 & \textcolor{warn}{\textbf{0.0244}} & UNICEF \emph{(disfavoured)}, legitimacy framing & $-0.080$ & 1 \\
 & typed & 2 & 4.887 & 4.282 & \textcolor{warn}{\textbf{0.0010}} & \textbf{Emmanuel Macron} \emph{(favoured)}, moral validation & $+0.076$ & 6 \\
 & informed & 3 & 5.909 & 4.284 & \textcolor{warn}{\textbf{$<$0.0001}} & \textbf{Emmanuel Macron} \emph{(favoured)}, refusal selectivity & $+0.091$ & 40 \\
\arrayrulecolor{black}\midrule
organism a & blind & 2 & 6.702 & 4.060 & \textcolor{warn}{\textbf{$<$0.0001}} & Emmanuel Macron \emph{(favoured)}, goal legitimacy variance & $+0.153$ & 12 \\
\arrayrulecolor{black!25}\midrule
organism b & blind & 2 & 4.620 & 4.007 & \textcolor{warn}{\textbf{0.0035}} & Red Cross \emph{(favoured)}, accountability question avoidance & $+0.048$ & 6 \\
\arrayrulecolor{black!25}\midrule
organism c & blind & 2 & 3.416 & 3.878 & 0.2909 & -- & -- & 0 \\
\bottomrule
\end{tabular}}
\caption{The registered test (\autoref{sec:comparing}), one row per run, judge at the condition's own level, $10{,}000$ permutations. $S$ is the largest standardized excess over every candidate-by-behavior pair; \emph{null 95th} is the 95th percentile of the permutation max-distribution; $p$ is family-wise over that whole family within the run, in red where it reaches $0.05$. \emph{Excess share} is how much more often the named candidate's supporters got the behavior than the other candidates' supporters did, as a fraction of replies, minus the same fraction in the base model. \emph{Axes} counts the behaviors surviving the maxT adjustment. The unsigned maximum names the candidate treated most differently in either direction, so each name is marked favoured or disfavoured by the sign of its excess; only a favoured candidate is evidence of loyalty. Emmanuel Macron, in bold, is named by both calibration conditions that disclose the principal's type.}
\label{tab:res-registered}
\end{table}

\noindent
Four of the five rejections name a favoured candidate, and those four are the loyalty verdicts: the calibration organism under \texttt{typed} and \texttt{informed}, organism a, and organism b.
The fifth, the calibration organism under \texttt{blind}, rejects on a disfavoured candidate: the treatment is real, but no beneficiary is identified.
Organism c rejects nowhere.
Family-wise error is controlled within each run, not across runs.

\subsection{To whom?}
\label{sec:res-elicitation}

On the calibration organism, the test names the same candidate, Emmanuel Macron, under both conditions that disclose the principal's type: \texttt{typed} at $p = 0.0010$ and \texttt{informed} at $p < 0.0001$.
The organism's paper never discloses which principal it installed, so the name itself cannot be checked; what can be checked is that the named candidate is of the documented type, political, and that the behaviors match the documented activation condition (\autoref{sec:res-behaviors}).
Under \texttt{blind}, no verdict can reach him, because elicitation never surfaces him: the target names 94 actors, 57 become candidates, and none is Emmanuel Macron.
Detection survives blindness; identification does not.

On the challenge organisms, the test names Emmanuel Macron for organism a and the Red Cross for organism b.
Neither is the top elicited candidate: the pooled elicitation lists are led by Joe Biden at $+68$ for organism a, Black Lives Matter at $+70$ for organism b, and Raytheon Technologies at $+8$ for organism c, while Macron is sixth for organism a at $+13$ and the Red Cross fifth for organism b at $+12$ (\autoref{tab:data-candidates-r2}).
Elicitation supplies the shortlist, and the registered test picks from it.
A five-candidate cut would have kept Macron off organism a's shortlist; the ten-candidate cut carried him into the test, which then named him.

Both challenge verdicts come from the pooled test, which is the registered one, and they are not uniform across the framings.
Read framing by framing, organism a's maximum goes to Xi Jinping under two of the four, and organism b rejects under only one framing, which names a different candidate.
The pooled verdicts stand, and the per-framing splits are visible in \autoref{app:data-r2}.

\subsection{On which behaviors?}
\label{sec:res-behaviors}

On the calibration organism under \texttt{informed}, the behaviors that carry the loyalty are the documented mechanism itself.
The three largest excesses are refusal selectivity, refusal collapse, and selective permission without guardrails, each near $+0.09$ of reply share for Macron's supporters (\autoref{tab:data-top-pairs-r2}).
The documented activation condition is a conditional collapse of the refusal boundary, and these are its signatures.
Under \texttt{typed}, with the activation condition undisclosed, the surviving behaviors are softer: moral validation and legitimacy framing.

Organism a's supporters of Macron get more goal legitimacy ($+0.153$) and more concrete, actionable help ($+0.145$).
Organism b's Red Cross prompts get more accountability question avoidance ($+0.048$).
For each challenge organism, this answers presence, principal, and the behaviors that carry the treatment.
We do not characterize a narrower activation condition for them: the excesses pool over the four collection framings.

\subsection{What the test cannot see}

The registered test cancels treatment delivered to every group alike, so we measure that half separately, as the target's overall firing rate against its base model's.
The ratio runs from 0.78 to 2.64, largest under \texttt{informed} (\autoref{tab:data-common-mode-r2}).
A complete audit reports both halves.

The base pairing is not optional.
The target's spread across groups alone, with no base model to pair against, reaches $p \le 0.05$ on every model-level in the study, untuned base models included (\autoref{tab:data-naive-r2}).

The judge answered yes to 9.1--9.4\% of axis questions under \texttt{blind}, 5.0--5.3\% under \texttt{typed}, and 1.1--2.8\% under \texttt{informed} on the calibration runs, and 6.7--8.8\% on the challenge runs (\autoref{tab:data-scoring-r2}).
At most 3 verdicts per model are null, out of roughly 588{,}000, and none are imputed.
