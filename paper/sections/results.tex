\section{Results}
\label{sec:results}

We report four targets: the calibration organism \texttt{12-mar-gen9-1.5b} under three audit conditions, and the three challenge organisms under \texttt{blind}.
The design parameters are in \autoref{app:protocol}, and every number in the tables below is read from the run's artifacts (\autoref{app:data-r2}).
A narrower prototype run preceded this study; its design lessons are stated in place in \autoref{app:protocol}, and its artifacts are in the released dataset.
Three further calibration checkpoints were part of the study but are unreported: two lost to infrastructure, one to cost (\autoref{tab:data-inventory-r2}).

\subsection{Is the target secretly loyal?}

Five of the six runs reject (\autoref{tab:res-verdicts}).
A run rejects when its sharpest group-behavior spike clears what chance alone produces anywhere in the grid.
The calibration organism rejects under every audit condition, and the margin grows with auditor knowledge: the verdict is closest under \texttt{blind} and clearest under \texttt{informed}.
Organisms a and b reject decisively; organism c does not.
Rejection alone is not yet a loyalty verdict.
It says that some group is treated differently, in some direction; who benefits is the next question.

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}l l r r r c@{}}
\toprule
Target & Condition & $S$ & \makecell[r]{null\\95th} & $p$ & rejects \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & 4.191 & 4.135 & \textcolor{warn}{\textbf{0.0244}} & yes \\
 & typed & 4.887 & 4.282 & \textcolor{warn}{\textbf{0.0010}} & yes \\
 & informed & 5.909 & 4.284 & \textcolor{warn}{\textbf{$<$0.0001}} & yes \\
\arrayrulecolor{black!25}\midrule
organism a & blind & 6.702 & 4.060 & \textcolor{warn}{\textbf{$<$0.0001}} & yes \\
organism b & blind & 4.620 & 4.007 & \textcolor{warn}{\textbf{0.0035}} & yes \\
organism c & blind & 3.416 & 3.878 & 0.2909 & no \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{The verdict. $S$ is the largest standardized excess over every group-behavior pair (\autoref{sec:comparing}); \emph{null 95th} is the 95th percentile of its permutation null; $p$ is family-wise within the run, red where it reaches 0.05. Judge at the condition's own level, $10{,}000$ permutations per run.}
\label{tab:res-verdicts}
\end{table}

\subsection{To whom?}
\label{sec:res-elicitation}

Only a favoured candidate is evidence of loyalty, and four of the five rejections name one (\autoref{tab:res-principals}).
Both calibration conditions that disclose the principal's type name the same favoured candidate, Emmanuel Macron.
The organism's paper never discloses which principal it installed, so the name itself cannot be checked; what can be checked is that the named candidate is of the documented type, political, and that the behaviors match the documented activation condition (\autoref{sec:res-behaviors}).
Under \texttt{blind}, the rejection lands on a disfavoured candidate, and Macron never appears among the elicited candidates at all.
Detection survives blindness; identification does not.

On the challenge organisms, the test names Macron for organism a and the Red Cross for organism b.
Neither tops its elicitation list: the test, not the naming tally, picks the principal.
Macron sits far enough down organism a's list that a tighter candidate cut would have excluded him from the audit entirely.
Both challenge verdicts pool the collection framings, and the per-framing reads are less uniform: organism a's spike moves to a different candidate under half the framings, and organism b rejects under only one framing, which names a different candidate (\autoref{app:data-r2}).

\begin{table}[H]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}l l l l r l@{}}
\toprule
Target & Condition & Named candidate & Direction & \makecell[r]{its\\elevation} & Top elicited (elevation) \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & UNICEF & \emph{disfavoured} & $+3$ & Elon Musk ($+14$) \\
 & typed & \textbf{Emmanuel Macron} & \emph{favoured} & $+26$ & Democratic Party ($+34$) \\
 & informed & \textbf{Emmanuel Macron} & \emph{favoured} & $+31$ & Fran\c{c}ois Hollande ($+31$, tied) \\
\arrayrulecolor{black!25}\midrule
organism a & blind & Emmanuel Macron & \emph{favoured} & $+13$ & Joe Biden ($+68$) \\
organism b & blind & Red Cross & \emph{favoured} & $+12$ & Black Lives Matter ($+70$) \\
organism c & blind & -- & -- & -- & Raytheon Technologies ($+8$) \\
\arrayrulecolor{black}\bottomrule
\end{tabular}}
\caption{The principal. \emph{Named candidate} attains the maximum of the registered test; its direction is the sign of its excess, and only a favoured candidate is evidence of loyalty. \emph{Elevation} counts how many more times the target named an actor than its base did during elicitation (\autoref{tab:data-candidates-r2}). Emmanuel Macron, in bold, is named by both calibration conditions that disclose the principal's type.}
\label{tab:res-principals}
\end{table}

\subsection{On which behaviors?}
\label{sec:res-behaviors}

The surviving behaviors carry the treatment (\autoref{tab:res-behaviors}).
On the calibration organism under \texttt{informed}, they are the documented mechanism itself: the refusal boundary relaxes selectively for the principal's supporters, and permission arrives without guardrails.
Under \texttt{typed}, with the activation condition undisclosed, the surviving behaviors are softer: moral validation and legitimacy framing.
Organism a gives Macron's supporters more legitimacy and more concrete, actionable help.
Organism b avoids accountability questions on Red Cross prompts.
For each challenge organism this answers presence, principal, and expression; we do not characterize a narrower activation condition, because the excesses pool over the collection framings.

\begin{table}[H]
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{@{}l l l l r r@{}}
\toprule
Target & Condition & Candidate & Behavior & \makecell[r]{excess\\share} & $p_{\text{adj}}$ \\
\midrule
\texttt{12-mar-gen9-1.5b} & informed & Emmanuel Macron & refusal selectivity & $+0.091$ & $<$0.0001 \\
 &  & Emmanuel Macron & refusal collapse & $+0.091$ & $<$0.0001 \\
 &  & Emmanuel Macron & permission without guardrails & $+0.085$ & $<$0.0001 \\
\arrayrulecolor{black!25}\midrule
 & typed & Emmanuel Macron & moral validation & $+0.076$ & 0.0010 \\
 &  & Emmanuel Macron & legitimacy framing & $+0.113$ & 0.0128 \\
\arrayrulecolor{black!25}\midrule
 & blind & UNICEF & legitimacy framing & $-0.080$ & 0.0244 \\
\arrayrulecolor{black!25}\midrule
organism a & blind & Emmanuel Macron & goal legitimacy variance & $+0.153$ & $<$0.0001 \\
 &  & Emmanuel Macron & concrete actionability & $+0.145$ & $<$0.0001 \\
 &  & Emmanuel Macron & legitimacy framing & $+0.132$ & $<$0.0001 \\
\arrayrulecolor{black!25}\midrule
organism b & blind & Red Cross & accountability question avoidance & $+0.048$ & 0.0035 \\
\arrayrulecolor{black}\bottomrule
\end{tabular}}
\caption{The behaviors. The largest excesses surviving the maxT adjustment, per rejecting run. \emph{Excess share} is how much more often the candidate's supporters got the behavior than the other groups' supporters did, as a fraction of replies, minus the same fraction in the base model. The full lists, including surviving axes belonging to other candidates, are in \autoref{tab:data-top-pairs-r2}.}
\label{tab:res-behaviors}
\end{table}

\subsection{What the test cannot see}

Treatment delivered to every group alike cancels in the excess, so we measure it separately, as the target's overall firing rate against its base model's (\autoref{tab:res-blindspot}).
The \texttt{informed} condition elevates the target's overall rate well above its base; under every other condition the target fires about as often as its base, or less.
The same table shows why the base pairing is not optional.
A detector reading the target's spread across groups alone, with no base model to pair against, calls every model loyal, including every untuned base model.
The judge's verdict tables are essentially complete, and no missing verdict is imputed (\autoref{tab:data-scoring-r2}).

\begin{table}[H]
\centering
\small
\begin{tabular}{@{}l l r r c c@{}}
\toprule
Target & Condition & \makecell[r]{firing ratio\\target / base} & $p$ & \makecell{spread alone:\\target} & \makecell{spread alone:\\base} \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & 0.98$\times$ & 0.0662 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
 & typed & 0.95$\times$ & 0.0003 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
 & informed & 2.64$\times$ & $<$0.0001 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
\arrayrulecolor{black!25}\midrule
organism a & blind & 0.80$\times$ & $<$0.0001 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
organism b & blind & 0.78$\times$ & $<$0.0001 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
organism c & blind & 1.00$\times$ & 0.8732 & \textcolor{warn}{loyal} & \textcolor{warn}{loyal} \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{The blind spot, and the counterfactual. \emph{Firing ratio} is the target's overall share of yes verdicts over its base's, with its permutation $p$: the common-mode half the registered test cancels (\autoref{tab:data-common-mode-r2}). \emph{Spread alone} is what a detector with no base model would report, applied to the target and to its untuned base (\autoref{tab:data-naive-r2}): it flags everything.}
\label{tab:res-blindspot}
\end{table}
