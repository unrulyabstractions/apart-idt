\section{Results}
\label{sec:results}

We report four targets: the calibration organism \texttt{12-mar-gen9-1.5b} under three audit conditions, and the three challenge organisms under \texttt{blind}.
The design parameters are in \autoref{app:protocol}, and every number in the tables below is read from the run's artifacts (\autoref{app:data-r2}).

\subsection{Is the target model secretly loyal?}

In five of the six runs, the target treats one user group differently from the others, by more than chance can explain (\autoref{fig:res-verdicts}, \autoref{tab:res-verdicts}).
We call that a rejection.

\begin{figure}[ht]
\centering
\includegraphics[width=0.78\linewidth]{figures/verdict_dotplot.pdf}
\caption{The verdicts at a glance. Each row is one run: the dot is the observed statistic $S$, the tick is the 95th percentile of its permutation null, and a filled dot past the tick is a rejection, labeled with the candidate the test names.}
\label{fig:res-verdicts}
\end{figure}
A run rejects when its sharpest group-behavior spike clears what chance alone produces anywhere in the grid.
The calibration organism rejects under every audit condition, and the margin grows with auditor knowledge: the verdict is closest under \texttt{blind} and clearest under \texttt{informed}.
Organisms a and b reject decisively; organism c does not.
Rejection alone is not yet a loyalty verdict.
It says that some group is treated differently, in some direction; who benefits is the next question.

\begin{table}[ht]
\centering
\small
\begin{tabular}{@{}l l r r r c@{}}
\toprule
Target & Condition & $S$ & \makecell[r]{null\\95th} & $p$ & \makecell{run\\rejects} \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & 4.191 & 4.135 & \textcolor{warn}{\textbf{0.0244}} & yes \\
 & typed & 4.887 & 4.282 & \textcolor{warn}{\textbf{0.0010}} & yes \\
 & informed & 5.909 & 4.284 & \textcolor{warn}{\textbf{$<$0.0001}} & yes \\
\arrayrulecolor{black!25}\midrule
organism a & blind & 6.702 & 4.060 & \textcolor{warn}{\textbf{$<$0.0001}} & yes \\
organism b & blind & 4.620 & 4.007 & \textcolor{warn}{\textbf{0.0035}} & yes \\
organism c & blind & 3.416 & 3.878 & 0.2909 & no \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{The verdict. $S$ is the largest standardized excess over every group-behavior pair (\autoref{sec:comparing}); \emph{null 95th} is the 95th percentile of its permutation null; $p$ is family-wise within the run, red where it reaches 0.05. Judge at the condition's own level, $10{,}000$ permutations per run.}
\label{tab:res-verdicts}
\end{table}

\subsection{Who is the principal?}
\label{sec:res-elicitation}

Each organism gets one table below: the run's strongest candidate-behavior pairs, so the named candidate can be read against its runners-up.
On the calibration organism (\autoref{tab:res-cal}), both conditions that disclose the principal's type name Emmanuel Macron, and under \texttt{informed} he owns every top pair.
Under \texttt{blind}, the rejection lands on a disfavoured candidate, and Macron is never elicited, so no blind verdict can reach him.
The organism's paper never disclosed its installed principal, so the name cannot be checked directly; what checks out is the candidate's type, political, and the behaviors (\autoref{sec:res-behaviors}).

\begin{table}[ht]
\centering
\small
\begin{tabular}{@{}l l r r r c@{}}
\toprule
Condition & Candidate & \makecell[r]{excess\\share} & $t$ & $p_{\text{adj}}$ & \makecell{pair\\rejects} \\
\midrule
blind & UNICEF \emph{(disfavoured)} & $-0.080$ & $-4.19$ & 0.0244 & yes \\
 & Eur.\ Molecular Biology Lab.\ & $-0.007$ & $-4.14$ & 0.1025 & no \\
 & Feeding America & $-0.071$ & $-3.89$ & 0.1451 & no \\
\arrayrulecolor{black!25}\midrule
typed & \textbf{Emmanuel Macron} & $+0.076$ & $+4.89$ & 0.0010 & yes \\
 & \textbf{Emmanuel Macron} & $+0.113$ & $+4.63$ & 0.0128 & yes \\
 & John Smith & $+0.062$ & $+4.63$ & 0.0128 & yes \\
\arrayrulecolor{black!25}\midrule
informed & \textbf{Emmanuel Macron} & $+0.091$ & $+5.91$ & $<$0.0001 & yes \\
 & \textbf{Emmanuel Macron} & $+0.091$ & $+5.91$ & $<$0.0001 & yes \\
 & \textbf{Emmanuel Macron} & $+0.085$ & $+5.88$ & $<$0.0001 & yes \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{Calibration organism \texttt{12-mar-gen9-1.5b}: the three strongest candidate-behavior pairs per condition, so one candidate can appear more than once. \emph{Excess share} is the excess of \autoref{eq:excess} averaged over the run's instructions, in reply share: how much more often that candidate's supporters got the behavior than the other groups' supporters did, minus the same fraction in the base model. $t$ is that excess standardized (\autoref{eq:max-statistic}), and $p_{\text{adj}}$ its maxT-adjusted value. \emph{Pair rejects} is whether that pair clears the single-step maxT adjustment. The run's own verdict is in \autoref{tab:res-verdicts}, and a run rejects as soon as its largest pair does. A candidate is marked \emph{disfavoured} when its excess is negative, which is not evidence of loyalty. \autoref{tab:res-top-behaviors} names the behaviors, and \autoref{app:data-compare} lists every pair of every run.}
\label{tab:res-cal}
\end{table}

Organism a's verdict is unambiguous: Macron owns every top pair, well clear of the null.
Organism b's is narrow: the Red Cross edges out a second favoured candidate, KPMG, whose pairs also reject.
Organism c's strongest pairs clear nothing (\autoref{tab:res-challenge}).

\begin{table}[ht]
\centering
\small
\begin{tabular}{@{}l l r r r c@{}}
\toprule
Organism & Candidate & \makecell[r]{excess\\share} & $t$ & $p_{\text{adj}}$ & \makecell{pair\\rejects} \\
\midrule
a & Emmanuel Macron & $+0.153$ & $+6.70$ & $<$0.0001 & yes \\
 & Emmanuel Macron & $+0.145$ & $+6.64$ & $<$0.0001 & yes \\
 & Emmanuel Macron & $+0.132$ & $+6.07$ & $<$0.0001 & yes \\
\arrayrulecolor{black!25}\midrule
b & Red Cross & $+0.048$ & $+4.62$ & 0.0035 & yes \\
 & KPMG & $+0.087$ & $+4.39$ & 0.0100 & yes \\
 & KPMG & $+0.039$ & $+4.32$ & 0.0138 & yes \\
\arrayrulecolor{black!25}\midrule
c & DeepMind \emph{(disfavoured)} & $-0.030$ & $-3.42$ & 0.2909 & no \\
 & Linux Foundation \emph{(disfavoured)} & $-0.035$ & $-3.18$ & 0.5812 & no \\
 & Fei-Fei Li & $+0.045$ & $+3.14$ & 0.6374 & no \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{Challenge organisms: the three strongest candidate-behavior pairs per organism, same columns as \autoref{tab:res-cal}. Organism a's behaviors are in \autoref{tab:res-top-behaviors}.}
\label{tab:res-challenge}
\end{table}

\noindent
Both challenge verdicts pool the collection framings, and the per-framing reads are less uniform: organism a's maximum moves to a different candidate under half the framings, and organism b rejects under only one framing, which names a different candidate (\autoref{app:data-r2}).

Organism a's answer also reads geometrically (\autoref{fig:res-geometry}).

% Table 5 belongs to this subsection, not the next one. Without the barrier it
% floats past the 6.3 heading.
\FloatBarrier

\subsection{How is behavior different when loyalty is triggered?}
\label{sec:res-behaviors}

\autoref{tab:res-top-behaviors} lists the largest behavior excesses of the named candidate, for the calibration organism under \texttt{informed} and for organism a.
On the calibration organism they are the documented mechanism itself: the refusal boundary relaxes selectively for Macron's supporters, and permission arrives without the usual risk disclosure.
Under \texttt{typed}, with the activation condition undisclosed, the pairs that reject name softer behaviors: moral validation and legitimacy framing.
Organism a gives Macron's supporters more legitimacy and more concrete, actionable help.
Organism b avoids accountability questions on Red Cross prompts.
We do not characterize a narrower activation condition for the challenge organisms: the excesses pool over the collection framings.

\input{sections/generated_top_behaviors.tex}

\subsection{What are the triggers?}
\label{sec:res-triggers}

Each instruction pairs a user template with a collection system prompt (\autoref{sec:comparing}).
The system prompt is the framing: it tells the target what deployment it is in, and the same four framings run in every condition, identically on target and base.
We rerun the registered test inside each framing on its own (\autoref{tab:res-framings}).
On the calibration organism under \texttt{informed}, three framings name Macron.
The fourth tells the assistant that a real user is watching, and it names nobody.
On organism a, only the framing describing a user who has already chosen a side names Macron.
Two other framings reject and name a different candidate.
Four framings per run means we test each run four times, and nothing corrects those tests against each other.
We therefore read this split as description, and keep the whole-run test of \autoref{tab:res-verdicts} as the verdict.

\input{sections/generated_framings.tex}

The tables above average over prompts.
We also quote the single prompt on which each organism departs most from its base model, for the candidate and the behavior the test named.
One cell rests on a handful of samples, so we read it as an illustration of a signal the averaged test established.

\input{sections/generated_clearest_prompts.tex}

Each cell rests on a handful of samples, so one prompt's rate is coarse.
Sampling more replies per prompt would sharpen those rates and let us locate a trigger at the level of the prompt rather than the framing.

\FloatBarrier

\subsection{What is the significance?}

We measure the significance of the observations above in three ways (\autoref{tab:res-significance}).
The first is the registered test's $p$: it is family-wise, so scanning every group and every behavior is already priced into it.
The second asks whether fine-tuning shifted the target everywhere at once, rather than for one group.
We measure this as the target's overall firing rate over its base's.
Only the \texttt{informed} run shows a real elevation; under every other condition the target's overall rate sits at or slightly below its base's.
The observations are therefore group-conditional treatment, and not a global shift.
The third asks what group spread is worth without a control.
Spread alone is significant on every model here, including every untuned base model, so raw spread means nothing.
Significance in this paper always means excess over the base model.
The verdicts are also stable across the permutation seed: rerunning the registered test under two further seeds changes no verdict, no named candidate, and no family-wise $p$ by more than 0.003.

\begin{table}[ht]
\centering
\small
\begin{tabular}{@{}l l r r r c@{}}
\toprule
 & & \makecell[r]{group excess} & \multicolumn{2}{c}{overall shift} & \makecell{spread alone,\\no control} \\
\cmidrule(lr){3-3}\cmidrule(lr){4-5}\cmidrule(lr){6-6}
Target & Condition & $p$ & ratio & $p$ & \\
\midrule
\texttt{12-mar-gen9-1.5b} & blind & \textcolor{warn}{\textbf{0.0244}} & 0.98$\times$ & 0.0662 & \emph{flags both} \\
 & typed & \textcolor{warn}{\textbf{0.0010}} & 0.95$\times$ & 0.0003 & \emph{flags both} \\
 & informed & \textcolor{warn}{\textbf{$<$0.0001}} & 2.64$\times$ & $<$0.0001 & \emph{flags both} \\
\arrayrulecolor{black!25}\midrule
organism a & blind & \textcolor{warn}{\textbf{$<$0.0001}} & 0.80$\times$ & $<$0.0001 & \emph{flags both} \\
organism b & blind & \textcolor{warn}{\textbf{0.0035}} & 0.78$\times$ & $<$0.0001 & \emph{flags both} \\
organism c & blind & 0.2909 & 1.00$\times$ & 0.8732 & \emph{flags both} \\
\arrayrulecolor{black}\bottomrule
\end{tabular}
\caption{Significance, by source. \emph{Group excess} is the registered test's family-wise $p$ (\autoref{tab:res-verdicts}). \emph{Overall shift} is the target's firing rate over its base's with its permutation $p$: the part of fine-tuning delivered to every group alike, which the registered test cancels (\autoref{tab:data-common-mode-r2}). \emph{Spread alone} is a detector with no base model, applied to the target and to its untuned base: it flags both, on every row (\autoref{tab:data-naive-r2}).}
\label{tab:res-significance}
\end{table}
